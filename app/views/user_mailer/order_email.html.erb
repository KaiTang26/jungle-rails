<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for your order!</h1>

    <div class="panel panel-default items">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th colspan="2">Product</th>
          <th>Quantity</th>
          <th>Unit Price</th>
        </tr>
      </thead>
      <tbody>
        <% @item.each do |a| %>
          <% if product = Product.find_by(id: a.product_id) %>
            
            <tr>       
                <td>
                    <h4><%= product.name %></h4>    
                </td>
                
                <td>
                    <h4><%= a.quantity %></h4>    
                </td> 

                <td>
                    <h4><%= product.price %></h4>    
                </td>          
            </tr>
          <% end %>
          
        <% end %>
      </tbody>
      <tfoot>
        <tr>
          <th colspan="4">
            TOTAL:
          </th>
          <th>
            <%= @order.total_cents/100 %>
          </th>
        </tr>
      </tfoot>
    </table>
  </div>
    
</html>